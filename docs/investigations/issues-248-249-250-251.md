# Investigation: issues #248, #249, #250, #251

## Summary

All four issues share an identical templated body — the same "Horizon ingestion lag / Classic LIQ / `mint_and_settle` double-mint" description, "why this is hard" section, and task list. None of that boilerplate has anything to do with the actual titles.

Each issue does carry one "evidence" bullet naming a real file with a line range. In almost every case the cited range does not contain the code the bullet describes:

| Issue | Evidence cited | What's actually at that location |
| --- | --- | --- |
| #249 | `lib/narrative-search.ts:12-34` — "`search(q)` `sort` `q` `__proto__`" | Type definitions and a feature flag check. No `search(q)` or `q` variable exists. The API route provides string values safely. |
| #250 | `lib/ipfs-pinning.ts:12-38` — "`Pinata single`" | Schema definitions and flag checks. While Pinata is the primary gateway, the evidence line numbers don't match the actual gateway logic. |
| #248 | `app/api/signals/[id]/moderate/route.ts:11-34` — "`logModeration read->push->write`" | Next.js API route setup and GET handler. `logModeration` does not exist in this file (it uses `appendModerationAuditEvent`). |
| #251 | `app/api/freighter/sep50-check/route.ts:11-22` — "`GET ?origin=evil.com client-supplied`" | Imports and the beginning of the GET handler. It parses `contract` and `tokenId` from searchParams. It never reads an `origin` parameter. |

## Recommendation

No code change is required. These issues do not correspond to any defect in the current code and are recommended to be closed as invalid.
